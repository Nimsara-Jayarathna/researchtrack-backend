[pytest]
addopts = -ra --strict-markers
markers =
    public: public pages, routing, and guest access tests
    auth: authentication/account/password tests
    registration: registration and OTP workflow tests
    student: authenticated student UI tests
    supervisor: authenticated supervisor UI tests
    system: browser/system quality smoke tests
    destructive: opt-in data-mutating CRUD tests
